server:
  port: 8081

spring:
  application:
    name: user-caller
  profiles:
    active: userCaller

# 注册中心地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8011/eureka/
  instance:
    prefer-ip-address: true
